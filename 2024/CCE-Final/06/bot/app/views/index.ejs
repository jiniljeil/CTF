<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>고객의 소리</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body style="background: #efefef">
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card p-3">
          <div class="card-body">
            <h5 class="card-title text-center">고객의 소리</h5>
            <form method="POST" action="/request">
              <div class="mb-3">
                <label for="url" class="form-label">문제 상황</label>
                <textarea class="form-control" id="description" name="description"></textarea>
                <br>  
                <label for="url" class="form-label">URL</label>
                <input type="text" class="form-control" id="url" name="url" placeholder="<%= prefix %>..." required>
              </div>
              <% if(pow) { %> 
              <div class="mb-3">
                <label for="pow_answer" class="form-label">Solve <code><%- pow.challenge_mask %> == "<%= pow.challenge %>"</code></label>
                <input type="text" class="form-control" id="pow_answer" name="pow_answer" required>
              </div>
              <% } %>
              <input type="hidden" name="sess_key" value="<%= sess_key %>">
              <div class="d-grid">
                <button type="submit" class="btn btn-primary">제출</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>